#escriba un programa que le pregunte al usuario el largo y el ancho de unahabitacion.
#una vez que los valores ha sido leido s, suprograma debe calcular y desplegar el area de la habitacion.
#El largo y ancho debe ser introducido con punto flotantev (decimal).
#incluya las unidades (metros) en su mensaje de entrada y salida.

largo= float (input('insertar el largo de la habitacion'))
ancho= float (input('insertar el ancho de la habitacion'))
area= print(largo*ancho)